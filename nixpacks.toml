# Nixpacks configuration for Railway deployment with Bun

[phases.setup]
nixPkgs = ["...", "bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "./yahoo-mcp"
